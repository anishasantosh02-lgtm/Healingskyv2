{
  "name": "llm-qa-agent",
  "version": "1.0.0",
  "description": "An LLM-driven QA tester agent that explores a web app with Playwright and judges pass/fail against plain-English acceptance criteria using Claude.",
  "type": "module",
  "scripts": {
    "test": "node index.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "dotenv": "^17.4.2",
    "openai": "^7.4.0",
    "playwright": "^1.47.0"
  }
}
